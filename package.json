{
  "name": "thermalserver",
  "version": "1.0.0",
  "description": "Server that makes CPU-intensive collision detection based on Serial input data from Arduino and streams back to Arduino and a front-end website served by this server.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marc Loewer",
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^4.18.2",
    "mqtt": "^5.3.3",
    "node-p5": "^1.0.4",
    "path": "^0.12.7",
    "ws": "^8.15.1"
  }
}
